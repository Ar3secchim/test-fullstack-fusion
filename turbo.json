{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": [".env"]
    },
    "start": {
      "dependsOn": ["^build"]
    }
  },
  "globalEnv": [".env"]
}