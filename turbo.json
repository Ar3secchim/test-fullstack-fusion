{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": [
        ".env"
      ]
    },
    "dev": {
      "cache": false,
      "env": [
        ".env"
      ]
    },
    "start": {
      "dependsOn": ["^build"],
      "env": [".env"]
    }
  },
  "globalEnv": [".env"]
}